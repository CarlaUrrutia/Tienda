spring:
  application:
    name: empleado
  profiles:
    active: dev

server:
  port: 8081

ms:
  tienda:
    url: http://localhost:8085

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html
