spring:
  application:
    name: producto
  profiles:
    active: dev

server:
  port: 8083

ms:
  proveedor:
    url: http://localhost:8084

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html
