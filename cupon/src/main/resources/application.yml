spring:
  application:
    name: cupon
  profiles:
    active: dev

server:
  port: 8090

ms:
  cliente:
    url: http://localhost:8082

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html
